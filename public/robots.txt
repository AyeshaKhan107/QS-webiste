# Applies to all search engines
User-agent: *

# Allow crawling of everything by default
Disallow:

# Block private or sensitive folders
Disallow: /admin/

# Optional: slow down crawling to reduce server load
Crawl-delay: 5

# Sitemap location (very important for SEO)
Sitemap: https://www.qamaradv-uae.com/sitemap.xml

# Optional hints (Google mainly uses sitemap, but you can hint important pages)
# Allow: /products/
# Allow: /blog/
